<%- include('layout') %>

    <h2>Books List</h2>

    <ul>
        <% books.forEach(book=> { %>
            <li><strong>
                    <%= book.title %>
                </strong> by <%= book.author %>
            </li>
            <% }) %>
    </ul>

    <a href="/books/add">Add New Book</a>